@model ChessModel.Board

<table>
    @for (var y = 0; y < 8; y++)
    {        
        <tr>
            @for (var x = 0; x < 8; x++)
            {
                <td>
                    @x @y
                </td>
            }
        </tr>
    }
</table>